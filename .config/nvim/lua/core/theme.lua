-- Colorscheme
require("nightfox").setup({
	options = {
		styles = {
			-- comments = "italic",
			-- keywords = "bold",
			-- types = "italic,bold",
			-- functions = "bold",
		},
	},
})

vim.cmd([[colorscheme nightfox]])
